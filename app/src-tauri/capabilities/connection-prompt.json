{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "connection-prompt",
  "description": "network decision prompt access",
  "windows": ["connection-prompt-*"],
  "permissions": [
    "core:default",
    "core:window:allow-close"
  ]
}
