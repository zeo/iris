{
  "$schema": "https://schema.tauri.app/config/2",
  "bundle": {
    "targets": ["deb", "rpm", "appimage"],
    "resources": {
      "../../target/release/iris-engine": "engine/iris-engine"
    },
    "linux": {
      "deb": {
        "files": {
          "/usr/libexec/iris/iris-engine": "../../target/release/iris-engine"
        },
        "depends": [
          "nftables",
          "libnetfilter-queue1",
          "libgtk-layer-shell0",
          "pkexec",
          "polkit-1-auth-agent | policykit-1"
        ]
      },
      "rpm": {
        "files": {
          "/usr/libexec/iris/iris-engine": "../../target/release/iris-engine"
        },
        "depends": ["nftables", "libnetfilter_queue", "gtk-layer-shell", "polkit"]
      }
    }
  }
}
